Commands to run for gitpy library installation:

python3 -m venv gitpy
source gitpy/bin/activate
pip install gitpython==2.1.7
export GIT_REPO_PATH='/your/repo/path'

On issue while running the script, execute the below command:

pip install gitpython --upgrade
